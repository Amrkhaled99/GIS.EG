import { DataRoundPipe } from './data-round.pipe';

describe('DataRoundPipe', () => {
  it('create an instance', () => {
    const pipe = new DataRoundPipe();
    expect(pipe).toBeTruthy();
  });
});
